#!/bin/bash
# Name of the job
#$ -N GPUtest
#$ -pe mpi 16
#$ -l gpu=1
#$ -q gpu 


date=$(date +%d_%m-%H%M)                                                                                                 

dir=gpu_$date
mkdir $dir
cp in.rigid data.vac job.sub $dir/.
cd $dir

echo $SGE_GPU

module load lammps
mpirun -np 1 lmp -k on -sf kk -in in.rigid
